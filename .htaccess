<IfModule mod_rewrite.c>
    RewriteEngine On
    # App runs under subdirectory on localhost
    RewriteBase /pln_up2d_ci_KP/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
